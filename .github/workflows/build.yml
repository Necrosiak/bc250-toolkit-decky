name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build plugin
        run: pnpm run build

      - name: Package plugin
        run: |
          mkdir -p out/BC250-Toolkit/dist
          cp dist/index.js out/BC250-Toolkit/dist/
          cp main.py updater.py bios_uma.py plugin.json package.json games_db.json games_catalog.json README.md out/BC250-Toolkit/
          # Déverrouillage 8 cœurs : notre sonde de statut + le script upstream de
          # rw-r-r-0644 avec sa licence MIT (obligatoire, et gardée byte-identique).
          cp -r core_unlock out/BC250-Toolkit/
          test -f out/BC250-Toolkit/core_unlock/upstream/LICENSE
          cd out
          zip -r ../BC250-Toolkit.zip BC250-Toolkit/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: BC250-Toolkit.zip
          generate_release_notes: true
